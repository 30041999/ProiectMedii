@page
@model ProiectMedii.Pages.Paintings.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Painting</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Painting.Name" class="control-label"></label>
                <input asp-for="Painting.Name" class="form-control" />
                <span asp-validation-for="Painting.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Painting.Artist" class="control-label"></label>
                <input asp-for="Painting.Artist" class="form-control" />
                <span asp-validation-for="Painting.Artist" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Painting.Price" class="control-label"></label>
                <input asp-for="Painting.Price" class="form-control" />
                <span asp-validation-for="Painting.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Painting.ExhibitionDate" class="control-label"></label>
                <input asp-for="Painting.ExhibitionDate" class="form-control" />
                <span asp-validation-for="Painting.ExhibitionDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Painting.ExhibitionID" class="control-label">Exhibition Name</label>
                <select asp-for="Painting.ExhibitionID" class="form-control" asp-items="ViewBag.ExhibitionID"></select>
                <span asp-validation-for="Painting.ExhibitionID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <div class="table">
                    <table>
                        <tr>
                            @{
                                int cnt = 0;
                                foreach (var cat in Model.AssignedCategoryDataList)
                                {
                                    if (cnt++ % 3 == 0)
                                    {
                                    @:</tr><tr>
                                    }
                                    @:<td>
                                        <input type="checkbox"
                                               name="selectedCategories"
                                               value="@cat.CategoryID"
                                               @(Html.Raw(cat.Assigned ? "checked=\"checked\"" : "")) />
                                        @cat.CategoryID @: @cat.Name
                                    @:</td>
                                }
                            @:</tr>
                            }
                        </table>
                    </div>
                </div>
                    <div class="form-group">
                        <input type="submit" value="Create" class="btn btn-primary" />
                    </div>
            </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
